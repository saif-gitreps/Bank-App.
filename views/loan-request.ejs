<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>Loan requests</title>
      <link rel="stylesheet" href="/styles/transfer.css" />
   </head>
   <body>
      <header>
         <h2>Loan requests</h2>
         <h2><a href="/admin-home/<%=admin%>">Back</a></h2>
      </header>
      <main>
         <ul>
            <% if(data.length == 0) {%>
            <h2>No requests.</h2>
            <%} else {%> <%for(let item of data) {%>
            <li>
               <h3>Borrower account:<%=item.account%></h3>
               <h3>Loan amount:<%=item.amount%> AED</h3>
               <form action="/loan/<%=item.id%>/accept" method="POST">
                  <button>Proceed Loan</button>
               </form>
               <form action="/loan/<%=item.id%>/reject" method="POST">
                  <button>Decline request</button>
               </form>
            </li>
            <%}%> <%}%>
         </ul>
      </main>
   </body>
</html>
